@*//===================================================================================
// © CBP - linkedin.com/in/
//===================================================================================*@

@using WebApp.Presentacion.WebMvc5.Resources

@model WebApp.Presentacion.WebMvc5.ViewModels.UserPasswordsCrudViewModel

@{ 
	ViewBag.Title = "Add UserPasswords"; 
}

<div class="row wrapper border-bottom white-bg page-heading">
    <div class="col-sm-4">
        <h2>@ViewBag.Title</h2>
        <ol class="breadcrumb">
            <li class="breadcrumb-item">
                <a href="@Url.Action("Index", "Home")">Home</a>
            </li>
            <li class="breadcrumb-item">
                <a>UserPasswords</a>
            </li>
            <li class="active breadcrumb-item">
                <strong>    @ViewBag.Title</strong>
            </li>
        </ol>
    </div>
</div>

<div class="wrapper wrapper-content">
    <div class="row">
        <div class="col-lg-12">
            <div class="ibox">
                <div class="ibox-content">

                    @using (Html.BeginForm())
                    {
                        @Html.AntiForgeryToken()

                        <div class="form-horizontal">
							@Html.ValidationSummary(true)

                            <div class="form-group">
                                @Html.LabelFor(model => model.UserPasswords.IdUsuario, new { @class = "control-label col-md-2" }):
								@Html.DropDownListFor(model => model.UserPasswords.IdUsuario, Model.Usuarios, new { @class = "form-control" })
								@Html.ValidationMessageFor(model => model.UserPasswords.IdUsuario, "", new { @class = "text-danger" })
							</div>
                            <div class="form-group">
                                @Html.LabelFor(model => model.UserPasswords.IdPassword, new { @class = "control-label col-md-2" }):
								@Html.DropDownListFor(model => model.UserPasswords.IdPassword, Model.Passwords, new { @class = "form-control" })
								@Html.ValidationMessageFor(model => model.UserPasswords.IdPassword, "", new { @class = "text-danger" })
							</div>

                            <div class="form-group">
                                @Html.LabelFor(model => model.UserPasswords.ExternalUser, new { @class = "control-label col-md-2" }):
								@Html.EditorFor(model => model.UserPasswords.ExternalUser, new { @class = "form-control" })
								@Html.ValidationMessageFor(model => model.UserPasswords.ExternalUser, "", new { @class = "text-danger" })
							</div>

							<div class="form-group">
                                <button type="button" onclick="submit()" class="btn btn-primary" data-toggle="tooltip" data-placement="top" title="@PresentationResources.Add">
                                    <i class="fa fa-save"> @PresentationResources.Add</i>
                                </button>

                                <a href="@Url.Action(PresentationResources.Find, "UserPasswords")" class="btn btn-white btn-sm" data-toggle="tooltip" data-placement="top" title="@PresentationResources.GoBack"><i class="fa fa-reply"></i> @PresentationResources.GoBack</a>
							</div>
						</div>
					}
                </div>
            </div>
        </div>
    </div>
</div>

